import Feed from '../App';
import React, {useEffect, useState} from 'react';

export default function ProfileScreen({navigation}) {
    return (
        <div>
            <span>Your profile will be displayed here.</span>
        </div>
    )
}